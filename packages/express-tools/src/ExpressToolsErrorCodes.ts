export enum ExpressToolsErrorCodes {
  /** server.listen() failed (EADDRINUSE, EACCES, etc.). Original err.code in details, original error as cause. */
  SERVER_LISTEN_FAILED = 'SERVER_LISTEN_FAILED',
}
